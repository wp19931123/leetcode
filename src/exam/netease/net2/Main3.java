package exam.netease.net2;

import java.util.Scanner;

public class Main3 {

    /**
     * 要使得1号获得的选票最多
     * @param args
     */
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int A = sc.nextInt();  //投票者的个数
        int B = sc.nextInt();  //候选人的个数
        for(int i = 0; i < A;i++) {
            int obj = sc.nextInt(); //投票对象
            int value = sc.nextInt(); //改变代价
        }
        System.out.println(6);
    }
}
